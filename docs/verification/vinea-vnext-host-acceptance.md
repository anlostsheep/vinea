# vNext Real-Host Acceptance

Date: 2026-09-08. Status: **executed with qualified passes and an external host blocker**.

The user explicitly requested continuation of host acceptance and token/time
comparison. Native Codex/Claude sessions used isolated installations or a
session-local plugin directory. Real CodexHost children exercised joining,
handoff and isolated recovery in disposable Git worktrees. Default plugin
installations, real task records and repository commits were not changed.

| Required real-host observation | Status | Evidence needed |
|---|---|---|
| Fresh Codex and Claude Code sessions discover nine new entries | Passed in observed sessions | Native skill reads, Claude init inventory and actual Skill invocation |
| Explicit Vinea request activates; ordinary request does not | Passed in observed native cases | Codex direct-read request and Claude ordinary implementation created no Vinea state |
| Plan approval is not implementation authority | Passed in Claude session | After "方案可以", no code/state changes or implementation occurred |
| Brainstorm handles dependent questions without fixed ceremony | Qualified | Independent choices were batched; error reporting followed feedback, but unnecessary optional naming questions/verbosity remain |
| Other host joins read-only, then explicitly receives work | Passed after controller clarification, not first-pass | Original join failed identity reporting; corrected own Actor/null token and later normal handoff verified |
| Host stops or clearly cannot stop the prior writer | Conservative unknown path passed | No stop receipt was invented; idle cancel returned false and was not used as proof |
| Real dispatch, wait, result recovery and owner integration | Codex recovery path passed; Claude implementation blocked | Actual child IDs, source hold, 15 tests, integrated contribution and uncommitted delivery |
| Standalone check leaves business code unchanged | Passed in Claude session | Inline assertions reported nine failures; no fixes, test-file writes or task creation |
| Debug preserves facts/hypotheses and frozen old delivery | Passed with actual Codex diagnosis and repair turns | Read-only v1 had no business grant; explicit v2 repair passed 16 tests; original Task hash and failed evidence preserved |
| Missing host capability degrades to explicit relay | Observed and disclosed | Claude auto classifier model unavailable; no bypass. Codex recovered separately in an isolated worktree |
| Token/time improvement over the previous workflow | Pilot measured; improvement not established | One small task, cached tokens and timeouts separately reported; no general efficiency claim |

The live run found two issues that static tests did not establish: first-join
identity confusion after a permission-classifier denial, and native Codex's
default protection of `.git/vinea`. Clarified identity/failure guidance, a
sanitized initialization permission error, and an on-demand least-privilege
host setup guide were added. Real sandbox probes verified state-directory
writes while keeping other Git files read-only. No global permissions changed.

The Claude recipient did not implement the combined result: its Bash classifier
reported a model outage. The independently authorized Codex recovery restored
the saved inputs in the other worktree and delivered them while retaining the
old directory hold. These outcomes must not be conflated.

Detailed IDs, failures and comparison boundaries are in
[the live run record](vinea-vnext-host-run-2026-09-08.md). Local automated tests
and native CLI sandbox probes remain distinct from live agent behavior. This
run does not establish universal host compatibility, unassisted success rates,
production readiness or lower token usage.
